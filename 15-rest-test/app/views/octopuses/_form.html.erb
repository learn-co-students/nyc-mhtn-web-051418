<%= form_with(model: octopus, local: true) do |form| %>
  <% if octopus.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(octopus.errors.count, "error") %> prohibited this octopus from being saved:</h2>

      <ul>
      <% octopus.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :legs %>
    <%= form.number_field :legs %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
