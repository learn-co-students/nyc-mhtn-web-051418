<%= form_with(model: goose, local: true) do |form| %>
  <% if goose.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(goose.errors.count, "error") %> prohibited this goose from being saved:</h2>

      <ul>
      <% goose.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :people_chased_today %>
    <%= form.number_field :people_chased_today %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
